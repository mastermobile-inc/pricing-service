---
spec_id: "site-service-requests-mvp"
title: "Site Service Requests MVP"
doc_type: spec
domain: "order_flow"
status: draft
owner: "engineering-and-operations"
source_of_truth: true
related_code:
  - app/api/site_service_requests.py
  - app/api/site_service_requests_ui.py
  - app/models/site_service_requests.py
  - app/services/site_service_request_conversations.py
  - app/services/site_service_requests.py
  - app/services/site_service_requests_worker.py
  - app/services/site_service_requests_ui_auth.py
  - integrations/master_mobile_site/service_ticket_bridge.php
  - scripts/ensure_site_service_requests_bitrix_process.py
  - scripts/ensure_site_service_requests_ui_placement.py
  - tasks/site_service_requests_worker.py
related_tests:
  - tests/test_ensure_site_service_requests_ui_placement.py
  - tests/test_site_service_request_conversations.py
  - tests/test_site_service_requests_api.py
  - tests/test_site_service_requests_auth.py
  - tests/test_site_service_requests_models.py
  - tests/test_site_service_requests_php_bridge.py
  - tests/test_site_service_requests_ui_api.py
  - tests/test_site_service_requests_worker.py
contracts:
  - openapi.yaml
depends_on: []
supersedes: []
rollout_required: true
updated_at: "2026-09-16"
---

# Назначение

Эта спецификация задаёт проверяемый контракт реализации задачи Bitrix24 №3223
«[Сайт][Сервис] Подключить гарантийные тикеты к CRM и рабочей очереди».
Задача №3223 является подзадачей №1077 по внедрению общего процесса рекламаций.

Бизнес-контекст и принятые командой правила описаны в
`docs/Onepage.SiteServiceRequestsMVP.md`. Настоящий документ является техническим
источником требований для кода, тестов и пилотного включения.

Цель MVP: каждый новый тикет из личного кабинета сайта должен без ручного
перепечатывания появиться в рабочей воронке смарт-процесса Bitrix24, получить
ответственного и срок первого ответа, а явный ответ сотрудника из карточки должен
вернуться в исходный тикет и стать видимым клиенту.

# Подтверждённое текущее состояние на 22.08.2026

## Сайт `master-mobile.ru`

- страница `/personal/tickets/` использует штатный модуль Bitrix `support` версии
  `26.0.0` и компоненты `bitrix:support.ticket.edit` /
  `bitrix:support.ticket.list`;
- заголовок, сообщения и файлы хранятся в `b_ticket`, `b_ticket_message` и
  `b_ticket_message_2_file`;
- поддерживаются события `OnAfterTicketAdd` и `OnAfterTicketUpdate`, в последнем
  доступен ID добавленного сообщения `MID`;
- форма сейчас содержит только тему, сообщение и один файл; лимит файла — 10 МБ;
- пользовательских полей сущности `SUPPORT` сейчас нет;
- обработчиков, передающих тикеты во внешнюю рабочую очередь, сейчас нет;
- в модуле 740 тикетов, 587 из них открыты, 347 созданы в 2026 году;
- сообщений с признаком `MESSAGE_BY_SUPPORT_TEAM=Y`, созданных в 2026 году, нет;
- все 740 тикетов назначены общей административной учётной записи сайта
  `MASTER MOBILE`; использовать эту учётную запись как интеграционного
  пользователя запрещено.

## Bitrix24 Box

- рабочий процесс уже существует: `entityTypeId=1134`, текущее название
  «Рекламации сайта / Браки сайта»;
- рабочая воронка: `categoryId=55`, «Рабочие рекламации»;
- архивная воронка: `categoryId=56`, «Архив старого чата»;
- рабочие стадии подтверждены живым REST-чтением:
  `NEW`, `PREPARATION`, `CLIENT`, `NEED_EXPERTISE`, `LINKED_EXPERTISE`,
  `REFUND_DECISION`, `SUCCESS`, `FAIL`;
- уже есть поля клиента, CRM-контакта, компании, сделки, номера заказа, описания,
  типа обращения, приоритета, срока реакции, решения, файлов клиента, связанной
  экспертизы, backend case ID и технического ключа идемпотентности;
- поле `UF_CRM_36_REACTIONDEADLINE` сейчас строковое и не используется как
  машинный datetime-контракт SLA;
- `timeman.status` и `timeman.settings` доступны для чтения по сотруднику;
- активные пользователи пилота подтверждены: Анна Гиря (`132252`), Ариф Рахманов
  (`12587`), Тимур Тибилов (`131016`), Александра Живых (`130904`). ID должны
  приходить из настроек, а не быть константами исходного кода.

## Существующий код `pricing-service`

- `app/services/site_defect_workflow.py` уже умеет читать рабочую воронку,
  классифицировать обращение, искать похожие архивные случаи, рассчитывать
  экономику возврата и предлагать связанные действия;
- `app/services/expertise_bitrix.py` содержит повторно используемый REST-клиент
  для `crm.item.*`, timeline, задач и Bitrix Disk;
- `scripts/ensure_site_defect_archive_bitrix_process.py` создаёт и сверяет поля,
  стадии и форму процесса 1134;
- при этом кода приёма новых тикетов сайта, двусторонней синхронизации ответа,
  TimeMan-распределения и durable dedupe пока нет;
- автоматическое создание отдельной задачи «Разобрать рекламацию» для каждого
  нового тикета в MVP не используется: рабочей единицей является сама карточка
  смарт-процесса.

# Scope / Out of Scope

## Входит в MVP задачи №3223

- три структурированных поля формы тикета: телефон, номер заказа и тип обращения;
- асинхронная передача только новых тикетов и новых сообщений после даты
  включения пилота;
- сохранение истории сообщений и входящих файлов;
- одна карточка процесса 1134 в воронке 55 на один тикет сайта;
- поиск или создание CRM-контакта без продажного лида;
- поиск существующей компании и сделки/заказа без создания дубликатов;
- назначение по фактически открытому рабочему дню Bitrix;
- SLA первого ответа и однократная эскалация Тимуру Тибилову;
- явная отправка текстового ответа из карточки в исходный тикет сайта;
- подтверждение факта появления ответа в личном кабинете;
- запрет окончательного закрытия карточки до подтверждённого ответа;
- health, метрики, журнал ошибок без текстов и контактов клиентов;
- feature flags, пилотный rollout и обратимый rollback.
- до пилота с сотрудниками смарт-процесс 1134 получает пользовательское название
  «Сервисные обращения сайта», а рабочая воронка 55 — «Рабочие обращения сайта»;
  `entityTypeId=1134`, ID воронок 55/56 и архивная воронка остаются неизменными;
- видимая сотрудникам форма использует понятные русские названия и подсказки:
  конечные наборы значений оформляются как enum/справочники, технические поля
  скрываются из основной формы, английские технические идентификаторы остаются
  только во внутреннем API и коде.

## Не входит

- перенос или массовое создание карточек по старым 587 открытым тикетам;
- сообщения из региональных и внутренних чатов;
- пункт контекстного меню «Создать сервисное обращение» в ChatApp/IM;
- автоматический перенос всех диалогов Открытых линий;
- ОТМЕНЕНО (2026-08-24): запрет на переименование живого смарт-процесса 1134;
  до пилота с сотрудниками принято пользовательское название «Сервисные обращения
  сайта» без изменения технического ID процесса и воронок;
- создание продажных лидов по сервисной теме;
- автоматическое создание экспертизы для каждого обращения;
- автоматическое денежное решение или проведение возврата в 1С;
- исходящие файлы сотрудника клиенту; в MVP исходящий ответ текстовый;
- отдельный часовой SLA для слова «срочно»;
- изменение публичного сайта, Bitrix24 или production без отдельного разрешения
  на rollout.

# Change Summary / Spec Delta

- было: тикет остаётся только в старом модуле поддержки сайта и назначается общей
  учётной записи;
- станет: сайт надёжно ставит событие в очередь, `pricing-service` создаёт или
  обновляет одну карточку 1134, назначает активного сотрудника, контролирует SLA и
  возвращает подтверждённый ответ в тикет;
- не меняется: личный кабинет и модуль `support` остаются клиентским источником
  переписки, процесс «Экспертиза» остаётся отдельным, 1С остаётся системой учёта
  возвратов и финансового факта.

# Accepted Service Email Intake 2026-08-25

Сервисные email-обращения из `shop` и `info` передаются в процесс 1134 только
при единственных точных контакте и заказе. Ответ выполняется в исходной
email-цепочке. Назначение и SLA наследуются от сервисной очереди; менеджер сделки
получает одно информационное уведомление. Межканальное объединение выполняется
только по технически подтверждённой связи.

Email-intake создаёт отдельный backend case на одну техническую почтовую цепочку,
если CRM-активность или исходящее письмо не содержит точной привязки к уже
существующей карточке 1134. По совпадению контакта и заказа разные каналы не
объединяются. Неоднозначное письмо остаётся CRM-активностью ручного разбора и не
создаёт продажный лид, контакт или сервисную карточку.

Ответственный выбирается действующей первой линией Анна Гиря / Ариф Рахманов по
открытой смене TimeMan; срок первого ответа и эскалация Тимуру остаются общими с
сайт-тикетами. Менеджер сделки получает одно информационное уведомление. Явный
запрос возврата денег сначала проверяет первая линия и не назначается Александре
Живых только по автоматической классификации письма.

Реализация использует `POST /api/internal/site-service-requests/email-events` с
существующей HMAC-защитой и
отдельным выключенным по умолчанию флагом
`SITE_SERVICE_REQUESTS_EMAIL_INGEST_ENABLED`. События `email.received` и
`email.replied` не содержат темы, тела, email или телефона. Зашифрованный
технический payload хранится в общей event-таблице до успешной обработки.

Универсальная таблица `site_service_request_source` задаёт соответствие
`bitrix-mail:<shop|info>:<threadId>` одной backend case. Сайт-тикеты получают
backfill `source_kind=site_ticket` и сохраняют прежние положительные
`source_ticket_id`; email-case использует детерминированный отрицательный
compatibility ID. При технической связи email-source добавляется к существующей
site case и не меняет её `site-support-ticket:<id>` idempotency key.

Email worker повторно читает `crm.activity.get`, сделку и контакт, проверяет
направление, thread, deal/contact bindings, точное поле номера заказа и единственный
email коммуникации контакта. Контакт и лид он не создаёт. После синхронизации
карточки worker добавляет activity binding к `entityTypeId=1134`, назначает
сервисную очередь, ставит deadline и выполняет readback. Поля карточки:
`mail_activity_id`, `mail_activity_url`, `mail_thread_key`. `email.replied`
фиксирует `first_response_at`, закрывает все входящие активности этой цепочки и
не меняет стадию сервисной карточки.

# Source of Truth

| Данные или правило | Источник истины |
| --- | --- |
| Клиентский тикет, сообщения и факт показа ответа | модуль `support` сайта |
| Доставка событий, dedupe, SLA, команды и техническое состояние синхронизации | БД `pricing-service` |
| Ответственный, стадия разбора, решение и связь с экспертизой | смарт-процесс 1134 |
| Клиент, компания и заказ/сделка | CRM Bitrix24 Box |
| Факт экспертизы | 1С и связанный процесс «Экспертиза» |
| Факт возврата денег | 1С/эквайринг по действующему процессу |

Смарт-процесс не является копией всей клиентской БД, а сайт не хранит состояние
рабочей очереди. Техническое состояние доставки не хранится только в cron или
логах.

# Архитектура и Data Flow

## 1. Мост на стороне сайта

Версионируемый исходник моста хранится в
`integrations/master_mobile_site/service_ticket_bridge.php`. При rollout он
подключается из отдельного файла `local/php_interface/include/`, а не вставляется
в большой `init.php` или `events.php`.

Мост выполняет четыре действия:

1. `OnAfterTicketAdd` и `OnAfterTicketUpdate` записывают минимальное событие в
   локальную таблицу `b_mm_service_ticket_outbox` и немедленно завершаются;
2. Bitrix Agent, запускаемый штатным `cron_events.php`, отправляет события во
   внутренний API `pricing-service`;
3. тот же Agent забирает ожидающие команды ответа;
4. команда ответа применяется через публичный API модуля `support`, после чего
   результат подтверждается `pricing-service`.

Клиентский POST формы никогда не ждёт CRM или `pricing-service`. Сетевая ошибка
не отменяет создание тикета и не показывает клиенту ошибку интеграции.

Таблица сайта `b_mm_service_ticket_outbox` содержит только:

- `id`;
- уникальный `event_key` вида `site-support:{ticket_id}:{message_id}`;
- `ticket_id`, `message_id`, `event_type`;
- `attempts`, `next_attempt_at`, `sent_at`;
- `last_http_status`, безопасный `last_error_code`;
- `created_at`, `updated_at`.

Текст сообщения, телефон, e-mail и содержимое файла в outbox не дублируются.
Они читаются из штатных таблиц только перед отправкой.
Строки `b_ticket_message` с `IS_LOG != 'N'` являются системным журналом и не
включаются ни в историю обращения, ни в fallback-выбор последнего сообщения.

## 2. Приём и очередь `pricing-service`

В `pricing-service` создаются таблицы:

### `site_service_request_case`

- внутренний `id`;
- уникальный `source_ticket_id`;
- `bitrix_item_id` с уникальным ограничением после создания карточки;
- найденные `crm_contact_id`, `crm_company_id`, `crm_deal_id`;
- `assigned_user_id`, `assignment_state`, `round_robin_seq`;
- `intake_mode`: `during_open_shift` или `outside_open_shift`;
- `first_seen_at`, `first_response_due_at`, `sla_paused_at`;
- `first_response_at`, `escalated_at`;
- `latest_inbound_message_id`, `latest_outbound_message_id`;
- `base_sync_status/base_error_code` хранят underlying результат без временного
  файлового overlay; `sync_status`, `last_error_code`, `version`;
- `created_at`, `updated_at`.

### `site_service_request_event`

- уникальный `event_id` из `event_key` сайта;
- `case_id`, `source_message_id`, `event_type`, `direction`;
- `payload_encrypted`, `payload_sha256`;
- `source_message_sha256` для устойчивого dedupe при изменении текущих полей
  карточки тикета между сетевыми повторами;
- `status`, `attempts`, `consecutive_permanent_failures`, `next_retry_at`,
  `last_error_code`;
- `processed_at`, `created_at`, `updated_at`.

Payload шифруется отдельным ключом
`SITE_SERVICE_REQUESTS_EVENT_ENCRYPTION_KEY`. После успешной синхронизации
`payload_encrypted` очищается, остаются только hash, IDs и результат доставки.

### `site_service_request_file`

- уникальная пара `source_message_id + source_file_id`;
- безопасное имя, MIME, размер, SHA-256;
- состояние загрузки;
- ID файла/объекта Bitrix после загрузки;
- `bitrix_error_reported_at` для retry/readback поздней файловой ошибки;
- временный путь хранится только до успешной загрузки и затем очищается.

### `site_service_request_command`

- `case_id`, уникальный `command_key`;
- зашифрованный текст ответа;
- `status`: `pending`, `leased`, `applied`, `failed`;
- `attempts`, `lease_until`, уникальный для каждой аренды `lease_token`;
- `source_message_id`, `ack_at`, `card_action_cleared_at`;
- `last_error_code`, `created_at`, `updated_at`.

Все изменения схемы `pricing-service` выполняются новой Alembic-миграцией.

Все транзакции, которые одновременно меняют case и command, используют единый
порядок row locks `case -> command`. Это относится также к выдаче lease и
quarantine повреждённой команды: предварительный выбор candidate case IDs не
блокирует строки, затем case блокируются в порядке ID, и только после них —
commands. Обратный порядок блокировок в этом контуре не допускается.

## 3. Создание или обновление карточки

Для нового события worker:

1. ищет `site_service_request_case` по `source_ticket_id`;
2. ищет карточку 1134 по техническому ключу
   `site-support-ticket:{ticket_id}`;
3. если карточка уже существует, связывает её с case и не создаёт новую;
4. если карточки нет, выполняет один `crm.item.add` в category 55/stage `NEW`;
5. после timeout или неоднозначного ответа сначала повторно читает карточку по
   техническому ключу и только затем решает, нужен ли новый `add`;
6. после timeout `crm.contact.add` также перечитывает контакт по отдельному
   `ORIGINATOR_ID + ORIGIN_ID`, а найденный ID подтверждает точным
   `crm.contact.get`; несколько или несовпадающие результаты завершаются
   fail-closed;
7. повторное клиентское сообщение обновляет историю и файлы этой же карточки.

В `crm.contact.get` поля ID, активности и компании проверяются до matching:
`COMPANY_ID/companyId` может быть только очищенным значением либо строгим
положительным ID; контейнеры, boolean, дробные и конфликтующие алиасы завершаются
fail-closed и не связывают обращение с компанией.

Полная история тикета передаётся как нормализованный snapshot и заменяет поле
истории целиком. Она не дописывается строкой, поэтому повтор события не создаёт
повторных сообщений. Каноническая история всё равно остаётся на сайте.

## 4. Ответ клиенту

ОТМЕНЕНО (2026-08-31): два равноправных способа ответа из CRM через старые поля
и непосредственно из новой вкладки создавали неоднозначный рабочий сценарий.
Единственным штатным CRM-каналом является вкладка «Переписка с клиентом».
Переход в исходный тикет остаётся ссылкой для проверки и аварийным ручным
fallback, но не основным сценарием работы в CRM. Служебная учётная запись
используется только фоновым Agent, её данные сотрудникам не передаются.

Старые поля `site_reply_text`, `site_reply_action`, `site_reply_status` и
`site_history` сохраняются для совместимости и аудита, но скрываются из общей
формы процесса 1134. Для существующих dynamic user fields production Bitrix Box
принудительно сохраняет `showInList=Y` даже после успешного
`userfieldconfig.update`; поэтому ensure отключает `showFilter` и `editInList`,
удаляет поля из управляемой формы и возвращает явный advisory для неизменяемого
`showInList`. Новые технические поля создаются с `showInList=N`. Legacy worker
обрабатывает `SEND` только при явном break-glass флаге
`SITE_SERVICE_REQUESTS_LEGACY_FIELD_REPLIES_ENABLED=true`;
по умолчанию флаг выключен. Перед rollout наличие незавершённых `SEND`
проверяется read-only, без автоматической отправки или очистки.

Основным рабочим местом сотрудника становится встроенная вкладка
`CRM_DYNAMIC_1134_DETAIL_TAB` «Переписка с клиентом» в карточке процесса 1134.
В ней сообщения клиента показываются слева, ответы поддержки справа, доступен
постоянный composer, шаблоны, исходящие вложения и отдельный режим внутренней
заметки. Внутри CRM ответ подписывается именем сотрудника и ролью поддержки;
клиент на сайте видит единый бренд «Поддержка MASTER MOBILE». Ссылка на исходный
тикет остаётся вторичным действием «Открыть обращение на сайте», а переход на сайт для
штатного ответа не требуется. Каноническая история остаётся на сайте; backend
хранит зашифрованную read-model и очищает её через 90 дней после подтверждённого
закрытия. В API вкладки `ticketId` показывает номер исходного тикета,
`canReply` учитывает source, feature flags и персональный write-allowlist, а
`canAttachFiles` отдельно отражает разрешение исходящих вложений. Все mutation
endpoints повторно проверяют ID сотрудника из подписанной item-scoped session по
`SITE_SERVICE_REQUESTS_UI_WRITE_ALLOWED_USER_IDS`; пустой список работает
fail-closed. ОТМЕНЕНО (2026-09-01): ограничение первого пилота только
`115204` и `131016`; после успешного controlled smoke write-allowlist
расширен до всего текущего view-allowlist: `115204`, `132252`, `12587`,
`131016`, `130904`. Новые пользователи портала автоматически право
записи не получают. Legacy-ответы, исходящие вложения и backfill
остаются выключены.

Для защиты от конкурентного редактирования outbound worker использует hash-guard
до и после очистки действия. Если текст изменился, `SEND` восстанавливается, а
следующий tick создаёт отдельную команду; новый UI/action ID в MVP не добавляется.
Восстановление считается успешным только после readback `SEND`, `PENDING` и того
же SHA-256 изменённого текста. Ошибка/timeout восстановления не завершает владение
предыдущей команды: следующий tick повторяет восстановление, поэтому новый текст
не теряется.
Поле ответа принимается только как строка: `list`, `dict`, `null` и другие
malformed-типы завершаются fail-closed до создания command. Пустой или состоящий
только из пробелов `SEND` не очищается: карточка получает `ERROR` и технический код
`reply_text_empty`, а health остаётся `degraded` до исправления текста. После
исправления следующий tick обрабатывает сохранённый `SEND` обычным hash-guard.
Ответ ограничен 200 000 символами, одинаково в worker и command API. Более длинный
`SEND` также не очищается, получает `ERROR`/`reply_text_too_long` и остаётся
`degraded` до сокращения текста. Старая oversized command-запись карантинируется
как `command_payload_invalid` до построения API response model, поэтому malformed
durable data не превращает весь `GET /commands` в ошибку валидации.
Перед lease SHA-256 расшифрованного текста обязан совпасть с durable
`reply_sha256`; несовпавшая command также карантинируется и не выдаётся сайту.
Quarantine обновляет outbound health-checkpoint только монотонно и не стирает
состояние более новой попытки. Успешный stale tick также не очищает
`outbound_last_error_code`, если сохранённый `outbound_checked_at` новее времени
его попытки; в success-ветке timestamp и error защищаются как единый
монотонный checkpoint.
Снятие action без исправления текста не скрывает `reply_text_empty` или
`reply_text_too_long`; checkpoint очищается только после чтения допустимого ответа
и начала создания command. Action
принимается только как строковый/целочисленный scalar либо пустое значение;
контейнеры и boolean завершаются fail-closed.
Повторный `SEND` с тем же текстом остаётся дубликатом, пока последняя команда
`pending/leased`. После завершённой `applied/failed` команды и подтверждённой
очистки действия тот же текст считается новым намерением и получает новую command.
После повторного захвата row lock worker сначала проверяет
`card_action_cleared_at`: если другая попытка уже завершила очистку, устаревшая
попытка не читает и не меняет карточку, а новый `SEND` остаётся следующему tick.
Terminal reconcile уже очищенной команды обновляет только status/sync-error и не
пишет action, поэтому конкурентный новый `SEND` остаётся следующему tick.

Site Agent получает команду, проверяет её повтор по `EXTERNAL_FIELD_1`, затем
вызывает `CTicket::Set`/`CTicket::SetTicket` со следующими условиями:

- используется отдельный активный пользователь сайта с минимальными правами
  команды поддержки;
- `MESSAGE_AUTHOR_USER_ID` и `MESSAGE_CREATED_USER_ID` равны ID этого пользователя;
- `EXTERNAL_ID` содержит числовой ID команды;
- `EXTERNAL_FIELD_1` содержит `mm-site-service-command:{command_id}`;
- проверка marker и `CTicket::Set` выполняются под MySQL advisory lock с тем же
  command ID; после ожидания блокировки lease повторно проверяется на срок действия;
- `CHECK_RIGHTS=N` допустим только внутри закрытого Agent, не в HTTP-обработчике;
- текст считается plain text: HTML удаляется, переносы строк сохраняются;
- команда может добавить сообщение только к ожидаемому `ticket_id`.

После получения `MID` Agent отправляет ack. `pricing-service` перечитывает событие
поддержки, фиксирует `first_response_at`, обновляет историю, очищает поле действия
и показывает статус «Отправлено». Если inbound event задержан, applied ACK уже
позволяет reconcile обновить только terminal-статус, не возвращая старой команде
владение очищенным action. Факт REST-ответа без `MID` и последующего readback не
считается доставкой клиенту.

Каждая выдача command получает новый случайный `leaseToken`. ACK принимается
только с токеном текущей аренды; запоздалый ACK предыдущей аренды получает
`409 command_lease_conflict` и не может завершить повторно выданную команду.
Повреждённый текст одной команды не блокирует выдачу валидных команд: строка
карантинируется только если ключ шифрования подтверждён другой успешно
аутентифицированной строкой; при возможной ошибке общего ключа выдача завершается
fail-closed без терминального изменения команд.

Повторная доставка той же команды на сайт возвращает уже существующий `MID` и не
создаёт второе сообщение. Advisory lock закрывает также пересечение старого Agent
с повторной выдачей после expiry: обе попытки не могут одновременно пройти
marker-readback перед `CTicket::Set`.

# API / Data Contracts

Все endpoints находятся под `/api/internal/site-service-requests/`, используют
HTTPS и отдельный secret. Bearer-токены других контуров не принимаются.

## 6. Ожидание ответа после первого

Срок первого ответа виден в карточке и управляет назначением, а дальше переписка
шла без единого признака: сотрудник не замечал, что клиент написал снова, и
обращение висело неотвеченным. На 15.09.2026 таких карточек было девять, самая
старая ждала с 2 сентября.

- `awaiting_reply_since` — момент первого сообщения клиента после последнего
  видимого ему ответа поддержки; в карточке это поле «Клиент ждёт ответа с».
  Внутренняя заметка ответом не считается;
- сообщение из одного приветствия ответом тоже не считается: «Здравствуйте!» без
  продолжения не закрывает срок первого ответа, не снимает ожидание клиента и не
  двигает карточку в «Ожидаем клиента». Критерий — `_is_substantive_support_reply`:
  снять приветствие из списка `SITE_SERVICE_GREETING_PREFIXES` и проверить, что
  осталось не меньше `SITE_SERVICE_MIN_REPLY_CHARS` символов. Порог живёт в коде,
  а не в `.env`: он напрямую двигает срок первого ответа. **Доставка при этом
  подтверждается по-прежнему для любого видимого сообщения** — иначе карточка
  осталась бы без статуса «Отправлено» и ответ ушёл бы клиенту второй раз;
  результат разведён в `SiteServiceRequestReplyReadback(delivered, substantive)`;
- пока первого ответа не было, отметка не ставится: этой паузой уже управляет SLA
  первого ответа, второй счётчик только задваивал бы уведомления;
- при появлении отметки ответственный получает уведомление, а карточка в стадии
  «Ожидаем клиента или товар» автоматически возвращается в работу: ожидание
  клиента закончилось, мяч у нас. Стадии берутся из `stage_map` ключами `client`
  и `work`; без них стадия не трогается;
- срок на повторный ответ тот же, что на первый (`first_response_hours`). По его
  истечении `escalate_overdue_site_service_replies` оставляет комментарий в
  карточке и уведомляет руководителя — один раз на одно ожидание, а не каждый тик;
- ответ клиенту снимает отметку и обнуляет признаки уведомления и эскалации;
- подтверждённый ответ переводит карточку из `new`/`work` в `client`: стадию иначе
  переключают руками, а значит забывают, и «В работе» перестаёт отличать обращения
  в работе от тех, где ждут клиента. Стадии экспертизы, возврата и закрытые не
  трогаются — там свой порядок;
- сообщение клиента в закрытую карточку возвращает её в `work`, снимает
  `closed_without_response_at` с причиной и оставляет в ленте пометку, что открыл её
  клиент. Иначе обращение остаётся в закрытых, где его никто не видит, а прежняя
  причина закрытия позволила бы закрыть карточку снова без ответа.

## 7. Автозакрытие обращений, где клиент замолчал

Ответ отправлен, клиент не возражает и больше не пишет — карточка остаётся
открытой навсегда, потому что закрыть её вручную некому: вопрос решён, и про
обращение просто забывают. Очередь при этом перестаёт показывать реальную работу.

- считается молчание от последнего **видимого клиенту** исходящего сообщения
  (`site_service_request_message`, `direction='outbound'`); внутренняя заметка
  отсчёт не начинает;
- срок задан в **рабочих днях** и берётся из общего производственного календаря
  `config/procurement-work-calendar.json` через `app/services/work_calendar.py`.
  Год, которого нет в календаре, не достраивается: лента оставляет карточку
  открытой с `errorCode="work_calendar_year_missing"` — закрыть по выдуманному
  сроку хуже, чем закрыть позже;
- закрываем строго из стадии `client`. «В работе», экспертиза и уже закрытые
  карточки не трогаются: там ждут не клиента, а нас;
- не закрывается карточка, по которой клиент ждёт ответа (`awaiting_reply_since`
  заполнен), нет первого ответа (`first_response_at` пуст) или закрытие уже
  сделано человеком (`closed_without_response_at`);
- факт автозакрытия хранится в отдельном поле `auto_closed_at`, а не в
  `closed_without_response_at`: там смысл другой — «закрыли без ответа клиенту», и
  подмена испортила бы статистику SLA и сломала бы переоткрытие карточки;
- гейт первого ответа такое закрытие не откатывает: обязательное условие отбора —
  заполненный `first_response_at`, а карточки с ним лента назначений не трогает;
- карточку, которую закрыть не удалось (например, она стоит в «В работе»),
  лента перепроверяет не чаще раза в час (`auto_close_checked_at`) — иначе одна
  такая карточка дёргала бы Битрикс каждую минуту и вытесняла бы остальные;
- в ленту карточки пишется пояснение с маркером `[site-service-auto-close:{case}]`.
  Комментарий отправляется **после** коммита отметки: сорванный комментарий
  оставит карточку без пояснения, но не приведёт к повторному закрытию;
- клиент, написавший в закрытую карточку, переоткрывает её сам — тем же правилом,
  что и для закрытия без ответа;
- срок настраивается `SITE_SERVICE_REQUESTS_AUTO_CLOSE_SILENCE_DAYS`, значение `0`
  выключает автозакрытие. Включать после разбора накопившихся карточек скриптом
  `scripts/backfill_site_service_stage_after_reply.py`, иначе первый же тик
  закроет их пачкой.

## 8. Дубли обращений

Клиент нажимает «отправить», не дожидается подтверждения, обновляет страницу и
отправляет снова — в очереди появляются две одинаковые карточки, и дальше их
разбирают два разных человека, не зная друг о друге. На истории обращений таких
пар три: разрывы 0:10, 2:34 и 9:20.

- дублем помечается **более новая** карточка; ничью по `first_seen_at` разрывает
  `id` — 759 и 760 создались в одну секунду;
- окно задаётся `SITE_SERVICE_REQUESTS_DUPLICATE_WINDOW_MINUTES`, `0` выключает
  отлов. Рабочее значение — **5 минут**: пары 0:10 и 2:34 попадают, пара 9:20
  остаётся открытой, потому что там клиент задал второй вопрос, а не повторил
  первый. При окне 10 минут эта пара закрылась бы ошибочно;
- каждое условие ниже отключает пометку, и все они обязательны: по новой карточке
  нет первого ответа и её не закрывали; по ней нет исходящих команд в
  `pending`/`leased`/`applied`; по ней не заведён незакрытый возврат; у обеих
  карточек **заполнен** один и тот же `crm_deal_id` (`NULL` не равен `NULL`);
  совпадает `source_kind` (письмо и тикет сайта по одному заказу — нормальная
  пара, там нужен человек); основная карточка открыта;
- тексты обращений не сравниваются: два действительно разных вопроса по одному
  заказу за пять минут — исчезающе редкий случай, а ошибка обратима;
- закрытие оформляется как административное: стадия `failure` и причина
  `close_without_response_reason = duplicate`. Побочный полезный эффект — SLA
  первого ответа по дублю перестаёт тикать и руководителя по нему не дёргают;
- обе карточки получают пояснение со ссылкой друг на друга, под защитой маркеров
  `[site-service-duplicate:{case}]` и `[site-service-duplicate-of:{case}]`;
  комментарии best effort и закрытие не откатывают;
- клиент, написавший в закрытый дубль, переоткроет его сам — стадия `failure`
  входит в закрытые, а причина закрытия при переоткрытии обнуляется;
- дубль ищется **после** сборки карточки: человеку полезно видеть её заполненной,
  с текстом обращения и ссылкой на основную.

## 9. Подсказка «Где заказ»

Около сорока процентов обращений — вопрос «где мой заказ». Ответ целиком лежит в
сделке: его заполняет приложение «Отправки» (otpravkee.me). Сотрудник каждый раз
открывает сделку, выбирает нужные строчки и перепечатывает их руками, из-за чего
ответы различаются по формату и в них попадает лишнее.

- `GET /items/{item_id}/order-status` отдаёт статус доставки и готовый текст
  клиенту. Права на запись не требуется: это чтение, и статус полезен всем, кто
  открыл карточку;
- сделка берётся из `site_service_request_case.crm_deal_id`, а если его нет —
  из самой карточки через `customer_return_service_requests`. Нет нигде —
  `404 order_status_deal_unknown`; недоступный портал — `503 order_status_unavailable`;
- поля сделки: `UF_CRM_OT_TRACKING`, `UF_CRM_OT_STATUS`, `UF_CRM_OT_TRACKING_LINK`,
  `UF_CRM_OT_PLANNED_DELIVERY_DATE`, `UF_CRM_OT_STORAGE_DATE`, номер заказа —
  `UF_CRM_1772784329053`. Даты приходят строками «дд.мм.гггг»;
- **прошедшие даты в текст не попадают**: «ожидаемая доставка 26.08» под статусом
  «Вручен» выглядит как ошибка сервиса;
- текст собирается на бэкенде, чтобы формат был один, покрывался тестом и в него
  не мог попасть внутренний код поля или идентификатор сделки;
- **при нескольких трек-номерах текст не собирается вовсе** (`multipleShipments`),
  UI вместо пункта показывает предупреждение: общий трек относится только к одной
  посылке — оговорка канона `site-order-fulfillment-control-contour.md`;
- нет ни статуса, ни трека — `customerMessage` пустой и пункт не показывается:
  «Здравствуйте! По вашему заказу…» без продолжения это бессодержательная отписка;
- во вкладке переписки статус грузится **один раз при открытии** и намеренно не
  участвует в опросе каждые 15 секунд: он меняется раз в сутки, а нагрузка на
  портал росла бы с каждой открытой карточкой.

## 5. Возврат товара из карточки

Возврат по обращению ведётся в реестре клиентских возвратов (задача №3507), а не
в полях карточки: у реестра есть состояния, история, сроки хранения и контроль
документа в 1С, и он же принимает возвраты без обращения. Поля карточки
`Перевозчик возврата`, `Трек-номер возврата`, `Дата создания трека` и
`Статус возврата` остаются вторым источником правды по той же посылке и выводятся
из формы.

Вкладка карточки «Возврат товара» — та же связь с другой стороны: менеджер видит
и заводит возврат, не покидая обращение.

- `GET /api/site-service-requests/ui/items/{item_id}/returns` — возвраты этого
  обращения: связь учитывается и по подтверждённому `service_request_item_id`, и по
  ранней привязке `bitrix_case_id`;
- `POST /api/site-service-requests/ui/items/{item_id}/returns` — регистрация
  возврата с `source="service_request_card"`. Карточка читается из Bitrix24, и
  возврат сразу получает тикет сайта, заказ и связь с обращением;
- повторная регистрация того же трека возвращает существующую запись со статусом
  `200` вместо создания второй: одна посылка — одна запись в реестре;
- право на запись — тот же список `site_service_requests_ui_write_allowed_user_ids`,
  что и для ответов клиенту; без него вкладка доступна только на чтение;
- трек чужого обращения или сделки отклоняется конфликтом реестра, а не
  перепривязывается молча.

Вкладка регистрируется тем же placement `CRM_DYNAMIC_1134_DETAIL_TAB`, что и
переписка, отдельным обработчиком `.../bitrix/site-service-requests/returns/`.
Ensure-скрипт принимает `--screen returns` и считает конфликтом только дубль
одного и того же обработчика: две вкладки одного приложения — штатная схема.

## Подпись запросов сайта

Обязательные headers:

- `X-MM-Site-Timestamp` — Unix time;
- `X-MM-Site-Nonce` — UUID;
- `X-MM-Site-Content-SHA256` — hex SHA-256 тела;
- `X-MM-Site-Signature` — `v1=<hex hmac-sha256>`.

Строка подписи:

```text
v1
<timestamp>
<nonce>
<HTTP_METHOD>
<PATH>
<content_sha256>
```

Допустимое расхождение времени — 300 секунд. Nonce принимается один раз в течение
10 минут. Сравнение подписи выполняется constant-time. Secret хранится только в
локальных env/Bitrix Option и не передаётся в URL или логи.

## `POST /events`

Минимальный payload schema v1:

```json
{
  "schemaVersion": 1,
  "eventId": "site-support:741:1201",
  "eventType": "ticket.created",
  "occurredAt": "2026-08-22T12:00:00+03:00",
  "ticket": {
    "id": 741,
    "siteId": "s1",
    "ownerUserId": 123,
    "title": "Не работает устройство",
    "phone": "+70000000000",
    "email": "client@example.invalid",
    "orderNumber": "000001",
    "requestType": "warranty",
    "isClosed": false
  },
  "history": [
    {
      "messageId": 1201,
      "authorKind": "customer",
      "createdAt": "2026-08-22T12:00:00+03:00",
      "text": "Описание проблемы",
      "files": []
    }
  ]
}
```

Допустимые `requestType`:

- `warranty` — гарантия/качество;
- `refund_money` — возврат денег;
- `replacement` — замена;
- `delivery_return` — доставка/возврат товара;
- `consultation` — консультация;
- `other` — другое.

Ответ `202`:

```json
{
  "eventId": "site-support:741:1201",
  "status": "accepted",
  "duplicate": false,
  "missingFileIds": []
}
```

Повтор с тем же `eventId` и тем же payload hash возвращает `duplicate=true`.
Изменение текущих полей тикета между повторами также считается duplicate, если
неизменны ID, время, автор, видимость, текст и набор file ID исходного сообщения.
Имя, MIME, размер и hash файла могут восстановиться вместе с удалённой записью
`b_file` и отдельно проверяются файловым endpoint.
Изменение самого исходного сообщения возвращает `409 event_payload_conflict`.
Старое значение `authorKind=support` принимается при readback истории вместе с
каноническим `support-team`, чтобы события предыдущей версии bridge продолжали
подтверждать доставленный ответ.
HTTP `413` не помечает строку site outbox как доставленную: полная история тикета
может временно превысить настроенный предел backend, поэтому событие остаётся
retryable до изменения лимита или исправления payload.

## `PUT /events/{event_id}/files/{file_id}`

Binary body загружается отдельно. Дополнительно передаются безопасное имя, MIME,
размер и SHA-256. Сервер сверяет размер/hash, запрещает path traversal и удаляет
временный файл после загрузки в Bitrix. Перед `unlink` cleanup блокирует сначала
case, затем файловую строку по durable file ID, и только после этого повторно
сравнивает статус/path. Поиск блокируемой строки только по `temporary_path`
запрещён: он не видит ещё незакоммиченный restage и может удалить его payload.
Переведённый обратно в `staged` файл владеет deterministic path и не удаляется
устаревшей cleanup-попыткой. Максимум MVP — 10 МБ на файл.

Если файл уже отсутствует или недоступен на сайте, bridge передаёт в event
нулевой SHA-256 placeholder и затем выполняет подписанный пустой `PUT` с
`X-MM-Site-File-Error: file_unavailable`. Backend сохраняет контролируемую
`file_sync_error`; если файл восстановился между повторами, только нулевой
placeholder позволяет атомарно заменить имя, MIME, размер и hash при полноценной
загрузке. Более поздний snapshot истории не меняет placeholder старого сообщения:
замена разрешена только бинарному `PUT` исходного event. Такой placeholder повторно
включается в `missingFileIds`. Временная
ошибка чтения/hash уже зарегистрированного файла не превращается в окончательный
`file_unavailable`: site outbox повторяет доставку. UTF-8 имя передаётся через
RFC 5987 `filename*` без конкурирующего ASCII fallback.

`SITE_SERVICE_REQUESTS_BITRIX_ROOT_FOLDER_ID` обязателен и должен быть
положительным, когда Bitrix writes включены. Отсутствующий/нулевой ID завершает
file worker configuration error до REST-записи. Disk lookup подтверждает точное
имя единственного файла и отклоняет malformed/пагинированный неоднозначный ответ.
После upload readback обязан сохранить все ранее связанные file IDs, не только
ID нового файла. При файловом overlay локальные `base_sync_status/base_error_code`
хранят underlying business/worker-состояние; Bitrix-статус восстанавливается
только для известного enum, а локальный underlying status — после успешного upload
в любом случае.

ID объекта служебного Bitrix Disk и ID файла CRM — разные идентификаторы и
хранятся раздельно: `bitrix_object_id` содержит Disk object ID, а
`bitrix_file_id` — CRM file ID из readback карточки. В multiple-file поле нельзя
передавать Disk object ID как число: Bitrix трактует его как чужой `b_file`.
Новый CRM-файл передаётся непосредственно парой `[имя, base64]`, а существующие
файлы сохраняются объектами `{"ID": <crm_file_id>}`. До записи worker скачивает
существующие вложения только по `urlMachine` того же webhook origin, ограничивает
размер и сверяет SHA-256, чтобы восстановить уже подтверждённый неизменённый файл.
ОТМЕНЕНО (2026-08-26): post-write SHA-256 больше не является универсальным
подтверждением доставки, потому что Bitrix может перепаковать JPEG и изменить его
байты при сохранении.

Перед первой попыткой добавить файл в CRM worker сохраняет в PostgreSQL
`bitrix_attach_attempted_at` и полный baseline существующих CRM file ID, затем
делает commit. После записи или неопределённого timeout доставкой считается только
появление ровно одного нового ID относительно сохранённого baseline; это правило
работает и для файлов, байты которых Bitrix преобразовал при сохранении. При
наличии marker и отсутствии подтверждённого `bitrix_file_id` разрешён только
readback: повторная REST-запись запрещена. Нулевой или множественный delta,
исчезновение baseline ID, malformed baseline, ошибка неопределённого readback или
превышение `SITE_SERVICE_REQUESTS_MAX_CRM_FILES_PER_ITEM` завершаются fail-closed
кодом `file_duplicate_guard`. Значение MVP — 50 файлов на карточку. При превышении
лимита worker не скачивает существующие файлы, не выполняет Disk upload и CRM
attach; spool сохраняется для ручного восстановления.

## `GET /commands`

Возвращает не более 20 pending-команд с lease на 5 минут и новым `leaseToken`.
Повтор после окончания lease допустим и безопасен за счёт `command_key`,
`EXTERNAL_FIELD_1` и ротации lease-token.
До выдачи проверяются UTF-8, непустой текст, предел длины и соответствие
`reply_sha256`; повреждённая строка не попадает в response.
Site bridge проверяет `schemaVersion` и полную форму каждой команды до записи в
тикет. Malformed command не подтверждается как `failed`: lease истекает, и ответ
остаётся доступен для повторной доставки.

## `POST /commands/{command_id}/ack`

Любой ack содержит `leaseToken` текущей аренды. Успешный ack дополнительно содержит
`ticketId`, `messageId`, `appliedAt`. Ошибка содержит только
стабильный код, например `ticket_not_found`, `support_user_invalid`,
`message_write_failed`; stack trace и содержимое сообщения не возвращаются.
Ответ ACK проверяется по `commandId`, `status` и boolean `duplicate`; HTTP `200`
с malformed JSON не считается подтверждением.

## `GET /health`

Возвращает количество pending/failed events, самый старый lag, pending commands,
непривязанные cases, ошибки assignment/outbound, незавершённые доставки эскалации
и время последнего успешного обмена. Любой ненулевой failure/delivery-счётчик
переводит health в `degraded`. Клиентские тексты, телефоны, e-mail, идентификаторы
карточек и имена файлов в health отсутствуют.

Durable checkpoint охватывает также ошибки lane до начала обработки карточки.
Карточка с assignment failure остаётся кандидатом reconcile после первого ответа,
чтобы успешный контрольный tick снял устаревший failure и вернул health в `healthy`.
При сбое выбора следующей карточки checkpoint сначала использует ещё не обработанную
в текущем tick запись, а после исчерпания списка — последнюю доступную fallback-запись;
поэтому успех первой карточки с тем же timestamp не скрывает последующую ошибку worker.
Checkpoint монотонен по `assignment_checked_at`/`outbound_checked_at`: failure от
старой попытки не перезаписывает более новый успешный tick и не создаёт ложный alert.
Поля API называются `assignmentFailures`, `outboundFailures` и
`pendingEscalationDeliveries`; ненулевые значения добавляют соответственно
`assignment_failure`, `outbound_failure`, `escalation_delivery_pending`.

Apply-worker хранит singleton heartbeat в PostgreSQL: `last_started_at` записывается
до configuration/user preflight, а `last_success_at` — только после полного
успешного tick. Исключение записывает безопасные `last_failure_at`,
`last_error_code`, `consecutive_failures`; тексты REST-ответов и клиентские данные
не сохраняются. Первая ошибка добавляет `worker_failure`, а отсутствие успешного
tick более `SITE_SERVICE_REQUESTS_WORKER_STALE_SECONDS` (MVP — 180 секунд) —
`worker_stale`; следующий полный успех сбрасывает failure. Failed file rows
возвращаются только агрегатом `failedFiles` и добавляют `file_failure`.

После захвата `flock` cron-wrapper ждёт
`SITE_SERVICE_REQUESTS_WORKER_START_DELAY_SECONDS` (MVP — 20 секунд, допустимо
0–55), чтобы не попадать в минутный Bitrix/WAF burst. Неструктурированный HTML
`HTTP 403 Access denied` повторяется после 2, 5 и 10 секунд. Структурированная
JSON-ошибка прав не повторяется и завершается fail-closed.

# Маппинг карточки 1134

## Существующие поля

| Данные | Поле Bitrix |
| --- | --- |
| Заголовок | `TITLE`: `Тикет сайта #<id> — <title>` |
| Источник | `UF_CRM_36_SOURCE`: `site-support-ticket` |
| Контакт вручную | `UF_CRM_36_CUSTOMERCONTACT` |
| CRM-контакт | `UF_CRM_36_CRMCONTACT` |
| CRM-компания | `UF_CRM_36_CRMCOMPANY` |
| Сделка/заказ | `UF_CRM_36_CRMDEAL` |
| Номер заказа | `UF_CRM_36_ORDERREFS` |
| Описание | `UF_CRM_36_PROBLEMDESCRIPTION` |
| Тип обращения | `UF_CRM_36_CUSTOMERREQUESTCHOICE` |
| Файлы | `UF_CRM_36_CLIENTFILES` |
| Backend case | `UF_CRM_36_BACKENDCASEID` |
| Dedupe | `UF_CRM_36_IDEMPOTENCYKEY` |

## Новые поля, создаваемые ensure-скриптом

| Логический ключ | Название | Тип |
| --- | --- | --- |
| `site_ticket_id` | ID тикета сайта | string |
| `site_ticket_url` | Открыть обращение на сайте | url/string |
| `site_history` | Переписка с клиентом на сайте | multiline string |
| `site_sync_status` | Что требуется по обращению | enumeration |
| `site_reply_text` | Ответ клиенту | multiline string |
| `site_reply_action` | Отправка ответа | enumeration: draft/send |
| `site_reply_status` | Доставка ответа клиенту | enumeration: none/pending/sent/error |
| `site_last_sync_at` | Последнее обновление с сайта | datetime |
| `first_response_due_at` | Ответить клиенту до | datetime |
| `first_response_at` | Ответ доставлен клиенту | datetime |
| `site_sync_error` | Техническая ошибка синхронизации | string, скрыто из основной формы |
| `return_decision_approved_by_user` | Кто согласовал решение | employee |

Существующее строковое поле `reaction_deadline` остаётся для совместимости, но
машинная логика использует только новое datetime-поле `first_response_due_at`.
Существующее строковое поле `return_decision_approved_by` также остаётся только
для совместимости и скрывается; сотрудник выбирается в новом поле типа `employee`.

Production Box 2026-08-24 подтвердил особенность `userfieldconfig`: русская
`editFormLabel` сохраняется и используется UI как основная подпись/fallback, а
`listColumnLabel` и `listFilterLabel` для полей процесса 1134 после update
возвращаются как `null`. Ensure требует и проверяет `editFormLabel`, а эти два
неподдерживаемых readback-поля сообщает как advisory и не блокирует форму.

Значения enum находятся через mapping, а не хардкодятся по числовому ID.

# Поиск клиента и заказа

1. Телефон нормализуется до цифр с ведущим кодом страны, e-mail — trim/lowercase.
2. `crm.duplicate.findbycomm` ищет контакт отдельно по телефону и e-mail. При
   наличии обоих результатов используется их непустое пересечение; конфликтующие
   непересекающиеся результаты объединяются и остаются неоднозначными.
3. Один совпавший активный контакт связывается автоматически.
4. Если по телефону/e-mail найдено несколько контактов, номер заказа используется
   как второй независимый ключ: worker ищет точное поле/точный токен заказа у
   сделок каждого кандидата. Ровно одна пара `контакт + сделка` безопасно разрешает
   дубль контакта; контакт и заказ связываются вместе.
5. Ноль или несколько подходящих пар не выбираются случайно: карточка получает
   `client_match_required`/`order_match_required`, продажный лид и новый контакт
   не создаются.
6. Если совпадений нет, создаётся обычный CRM-контакт с источником
   `site-service-request`; продажный лид не создаётся.
7. Компания связывается только если она уже однозначно связана с контактом;
   новую компанию MVP не создаёт.
8. Сделка ищется по точному номеру заказа в настраиваемом поле
   `SITE_SERVICE_REQUESTS_CRM_ORDER_FIELD`. Для пилота кандидатом является
   наблюдаемое поле `UF_CRM_1772784329053`, заполненное числом у последних
   интернет-заказов. Его соответствие номеру сайта подтверждено read-only на
   реальных заказах 241094 и 241573; production env должен содержать это поле до
   повторного запуска intake.
9. Fallback — единственная сделка контакта, в заголовке которой есть точный
   токен номера заказа. Подстрока без границ числа совпадением не считается.
10. Ноль совпадений оставляет номер текстом и ставит `order_not_found`; несколько
   совпадений ставят `order_match_required`. Случайная сделка не выбирается.

Пользовательская форма не показывает внутренние коды. Поле с исходными телефоном
и e-mail отображается многострочно, конечные состояния имеют русские
действие-ориентированные подписи: например «Готово к работе», «Найдено несколько
клиентов — выберите нужного» и «Заказ не найден — проверьте номер».

# Назначение и SLA

Настройки пилота:

- первая линия: Анна Гиря и Ариф Рахманов;
- резерв/эскалация: Тимур Тибилов;
- финансы/возвраты: Александра Живых;
- timezone: `Europe/Moscow`;
- срок первого ответа: 4 рабочих часа;
- вне рабочего времени: до 12:00 следующего фактически начавшегося рабочего дня;
- отдельного срочного SLA нет.

Все user IDs задаются env-настройками. Перед стартом worker проверяет, что каждый
ID существует, активен и соответствует ожидаемому имени/роли.

## Правило назначения

- новым назначениям доступен сотрудник с `timeman.status.STATUS=OPENED`;
- `PAUSED`, `CLOSED`, `EXPIRED`, неактивный пользователь или ошибка TimeMan не
  считаются доступностью для нового назначения;
- `STATUS` TimeMan принимается только как известное строковое значение; malformed
  или неизвестный статус трактуется как `ERROR`, сохраняет
  `assignment_last_error_code=timeman_unavailable` и деградирует health;
- работает один — назначить ему;
- работают оба — round-robin по последнему успешному назначению в БД под
  транзакционной блокировкой;
- не работает никто — карточка остаётся `NEW` без ответственного первой линии;
- worker повторяет проверку не реже одного раза в минуту;
- автоматическое переназначение разрешено, пока карточка находится в стадии `NEW`
  и `first_response_at` отсутствует;
- после ухода карточки из `NEW` ответственный закрепляется; дальнейшая смена
  выполняется вручную либо при SLA-эскалации.

Перед `crm.item.update` worker сравнивает управляемые поля с последним readback.
Даты сравниваются в UTC с точностью до секунды. В Bitrix передаются только
изменившиеся поля; при полном совпадении write пропускается. После фактической
записи сохраняется обязательный readback.

## Расчёт SLA

- если тикет пришёл при хотя бы одном `OPENED`, базовый due равен
  `received_at + 4 часа`;
- если затем ни один сотрудник первой линии не `OPENED`, SLA-clock ставится на
  паузу, а после возобновления due сдвигается на длительность паузы;
- если тикет пришёл без открытой смены, при первом следующем `OPENED` due
  устанавливается на 12:00 по Москве этой даты;
- если первая смена открыта после 12:00, обращение сразу считается просроченным;
- ОТМЕНЕНО (2026-08-30): первый ответ засчитывается только по successful site
  ack/readback. Ответ из CRM по-прежнему требует successful site ack/readback;
  прямой ответ сотрудника в исходном тикете засчитывается после появления в
  синхронизированной истории видимого клиенту сообщения команды поддержки;
- просрочка без `first_response_at` один раз переводит ответственность Тимуру,
  добавляет timeline-комментарий и уведомление;
- напоминания сотрудникам формулируются простым русским языком без технических
  сокращений: при просрочке указываются письмо или обращение, факт истечения
  срока ответа и понятное действие — проверить обращение и ответить клиенту;
- напоминание содержит прямую ссылку на карточку обращения в CRM; для письма
  дополнительно указывается ссылка на исходное письмо, для обращения сайта —
  ссылка на исходное обращение на сайте, если она доступна;
- ОТМЕНЕНО (2026-08-29): правило показывать в ежедневной сводке все обращения
  сайта без первого ответа. Причина: непросроченные обращения создают лишний шум;
- ежедневно после `11:00 Europe/Moscow` в чат задачи №372 отправляется отдельная
  сводка только по письмам без ответа и просроченным обращениям сайта;
- каждый пункт формулируется простым действием для сотрудника: открыть письмо или
  карточку обращения и ответить клиенту. Добавляются прямые ссылки на карточку CRM
  и исходное письмо или обращение, если соответствующая ссылка доступна;
- непросроченные обращения сайта, сведения об интернет-заказах, технические
  статусы и ошибки синхронизации в сводку не включаются;
- если писем без ответа и просроченных обращений сайта нет, ежедневная сводка явно
  сообщает: `Обращений, требующих ответа, нет`;
- доставка ежедневной сводки защищена датой в локальном состоянии и проверкой
  сообщения в чате: повторный или восстановительный запуск не создаёт дубль;
- решение об эскалации фиксируется локально в `escalated_at` до внешних эффектов;
- timeline доставляется с детерминированным маркером и readback, а локальные
  `escalation_*_delivered_at` позволяют безопасно продолжить после timeout;
- для smart-process timeline используется REST-тип `dynamic_<entityTypeId>`, а
  поиск маркера проходит все страницы результатов;
- уведомление повторяется до локального подтверждения успешного вызова с одним
  стабильным `TAG=mm-site-service-escalation:{case_id}:{user_id}`. Bitrix объединяет
  повтор с тем же tag, поэтому timeout после фактического принятия не создаёт второе
  уведомление. `im.notify.personal.add` считается успешным только при строгом
  положительном ID в `result`; пустой или malformed result оставляет доставку
  pending для retry с тем же tag.

Маркер «срочно» может влиять на визуальный приоритет, но не сокращает SLA MVP.

# Invariants

- один тикет сайта соответствует одной case и максимум одной карточке 1134;
- одно сообщение сайта соответствует одному event ID;
- один command ID создаёт не более одного сообщения поддержки на сайте;
- создание тикета клиентом не зависит от доступности CRM/`pricing-service`;
- ни одно сервисное обращение не создаёт продажный лид;
- внутренние и региональные чаты не попадают в этот pipeline;
- `categoryId=56` и старые архивные карточки не изменяются;
- закрытие карточки без `first_response_at` автоматически отменяется не позднее
  следующего worker tick; карточка возвращается в последнюю незакрытую стадию;
- исключение — административное закрытие: если заполнено поле
  `close_without_response_reason`, worker принимает закрытие, фиксирует
  `closed_without_response_at` и причину и больше не трогает карточку. Нераспознанное
  значение причины гейт не открывает;
- отправленный из карточки ответ удерживает гейт раньше, чем событие с сайта
  проставит `first_response_at`: команда ответа в статусе `applied` делает закрытие
  законным, а `pending`/`leased` моложе `command_lease_seconds` откладывает решение
  по стадии до следующего тика. Зависшая дольше аренды или `failed` команда гейт
  больше не удерживает;
- откат закрытия не молчит: гейт оставляет в карточке комментарий с причиной и двумя
  выходами (ответить клиенту либо заполнить причину). Сбой этого комментария сам
  откат не отменяет;
- `closed_without_response_at` не подменяет `first_response_at`: в SLA такая карточка
  остаётся обращением без ответа клиенту;
- принадлежность письма цепочке подтверждает почтовый диспетчер по БД портала:
  Bitrix24 Box не отдаёт `THREAD_ID` через REST, поэтому REST-сверка выполняется
  только когда портал фактически вернул поле;
- финансовое решение остаётся человеческим; код только маршрутизирует и фиксирует;
- тексты сообщений, телефоны, e-mail, webhook и подписи не попадают в обычные логи;
- production writes, миграции сайта, поля Bitrix и deploy выполняются только после
  отдельного разрешения и с readback.

# Errors / Edge Cases

- timeout после `crm.item.add`: сначала readback по idempotency key, без слепого
  повторного `add`;
- HTTP `429/500/502/503/504`: повторы через 1, 2, 5, 15 и 30 минут, далее не чаще
  одного раза в час до 24 часов;
- site outbox считает terminal только явные payload/contract HTTP
  `400/409/415/422`; HTTP `413`, auth/routing/outage ответы остаются retryable,
  чтобы исправление лимита, настроек или deploy не потеряло уже созданный тикет;
- `202/200` считается успехом только после строгой проверки JSON response,
  типов идентификаторов, статуса, boolean `duplicate` и обязательного списка файлов;
  неизвестный file ID, повтор ID или отсутствие `missingFileIds` оставляет outbox
  на retry;
- ошибка SQL/readback локального site outbox, тикета, истории или файлов не
  маскируется как пустой результат и остаётся retryable `site_database_unavailable`;
- событие старше 24 часов или пять последовательных permanent errors получает
  `needs_attention` и попадает в health/внутреннее уведомление;
- transient failure и успешная обработка сбрасывают счётчик последовательных
  permanent errors;
- невалидная подпись, старый timestamp, повтор nonce и hash conflict не
  ретраятся как временная ошибка;
- недоступный TimeMan не приводит к случайному назначению: карточка остаётся
  `NEW`, технический статус — `assignment_waiting`;
- неизвестный клиент или заказ не мешает создать сервисную карточку, но требует
  ручного связывания;
- входящий файл с неверным размером/hash отклоняется, обращение всё равно создаётся
  с `file_sync_error` и ссылкой на исходный тикет;
- одинаковые имена файлов не являются dedupe-ключом;
- изменение ответа после постановки команды сохраняет/восстанавливает `SEND` и
  создаёт новую команду на следующем worker tick;
- временная недоступность `CFile`, пустой result `CFile::GetPath`, пустой document
  root, ошибка `hash_file` или исчезновение файла между event и upload оставляют
  site outbox retryable и не создают ложный zero-hash placeholder;
- malformed/пустой ответ не создаёт command: неверный тип завершается fail-closed,
  а пустой `SEND` получает явный `ERROR`/`reply_text_empty` без очистки action;
- offsets Bitrix pagination принимаются только как `int` или строка из ASCII-цифр;
  дробные, логические, отрицательные и форматированные пробелами значения
  завершаются fail-closed без перехода на потенциально неверную страницу;
- IDs типа процесса в ensure используют тот же строгий integer contract, а
  `fieldName`/`userTypeId`, имена секций/элементов формы, `ACTIVE` контакта,
  `stageId`, `TITLE` сделки и `COMMENT` принимаются только как строки; страница
  timeline принимается только как list. Malformed значения не участвуют в mapping,
  matching, marker readback и не записываются обратно в Bitrix;
- CRM deal/contact/item/Disk IDs с aliases `ID/id` принимаются только при одинаковом
  положительном целом значении; конфликт завершается fail-closed до выбора или write;
- ensure проверяет `xmlId/XML_ID` у каждой прочитанной строки поля, а целевое поле
  с ожидаемым `fieldName` принимает только при точном строковом `xmlId`; stage ID
  должен быть строкой текущего scope `DT<entityTypeId>_<categoryId>:`. Контейнеры,
  конфликтующие алиасы и stage из другой category завершают проверку до любых writes;
- ambiguous DB commit после атомарной записи spool-файла удаляет payload только
  под блокировками `case -> file`, взятыми по `event_id + source_file_id`; readback
  по одному `temporary_path` недостаточен. При недоступной или неоднозначной
  идентичности файл остаётся fail-safe;
- прямой ответ сотрудника через штатную админку support также ловится событием и
  может закрыть SLA, если сообщение подтверждено как support-team;
- событие собственного автоматического ответа распознаётся по
  `EXTERNAL_FIELD_1` и не создаёт цикл.

# Acceptance Criteria

- [ ] Новые поля тикета видны в личном кабинете, телефон и тип обязательны,
  номер заказа сохраняется отдельно.
- [ ] Создание тикета успешно при полностью недоступном `pricing-service`, а
  событие остаётся в site outbox.
- [ ] Новый тикет появляется в category 55 не позднее 5 минут после восстановления
  связи.
- [ ] Повтор одного event 10 раз создаёт одну case и одну карточку.
- [ ] Второе сообщение клиента обновляет ту же карточку и полную историю без дубля.
- [ ] Входящий файл до 10 МБ открывается сотрудником из карточки либо фиксируется
  контролируемая `file_sync_error` со ссылкой на тикет.
- [ ] Уникальный существующий CRM-контакт связывается без нового лида.
- [ ] Новый клиент получает контакт и сервисную карточку, но не лид.
- [ ] Несколько совпавших контактов/заказов не выбираются случайно.
- [ ] Контрольный номер заказа связывается с правильной сделкой по точному полю.
- [ ] При открытой смене только Анны карточка назначается Анне.
- [ ] При открытой смене только Арифа карточка назначается Арифу.
- [ ] При двух открытых сменах последовательные карточки распределяются по очереди.
- [ ] При отсутствии открытых смен карточка остаётся `NEW` без случайного
  ответственного и назначается после открытия смены.
- [ ] Срок при обращении вне смены равен 12:00 первого дня с открывшейся сменой.
- [ ] Просрочка эскалируется Тимуру ровно один раз.
- [ ] Выбор «Отправить клиенту» создаёт один ответ в исходном тикете.
- [ ] Пустой или более длинный чем 200 000 символов `SEND` не создаёт command,
  не очищает action и сохраняет явную degraded-ошибку до исправления текста.
- [ ] HTTP `413` от event endpoint не выставляет `sent_at` в site outbox и
  повторяется после backoff.
- [ ] Клиент видит текст ответа в `/personal/tickets/?ID=<ticket_id>`.
- [ ] Повтор command/ack не создаёт второе сообщение.
- [ ] Карточка без подтверждённого ответа не остаётся в `SUCCESS`.
- [ ] Архив category 56, старые 587 тикетов и региональные чаты не изменены.
- [ ] Health показывает lag и ошибки без персональных данных.
- [ ] Отключение feature flags прекращает новые записи, не ломая личный кабинет.

# Implementation Checklist

- [x] Добавить модели и Alembic-миграцию `pricing-service`.
- [x] Добавить dedicated settings, HMAC/replay protection и `.env.example` без
  реальных секретов.
- [x] Добавить schemas и внутренний API events/files/commands/ack/health.
- [x] Добавить сервисы dedupe, CRM matching, Bitrix item sync, TimeMan assignment,
  SLA и outbound commands.
- [x] Переиспользовать `BitrixRestClient`, не создавать второй несовместимый REST-клиент.
- [x] Расширить ensure-скрипт полями сайта и формой рабочей воронки.
- [x] Отключить создание отдельной задачи «Разобрать рекламацию» в этом intake-flow.
- [x] Добавить worker и зарегистрировать его в CLI/cron governance с dry-run.
- [x] Добавить версионируемый PHP bridge, idempotent DDL и Agent.
- [x] Добавить SUPPORT user fields и вывести их через `SET_SHOW_USER_FIELD` /
  кастомный шаблон.
- [x] Добавить feature flags отдельно для site emit, backend ingest, Bitrix writes
  и outbound replies.
- [x] Добавить метрики, безопасные error codes и alert на lag/dead-letter.
- [x] Обновить OpenAPI после реализации API.
- [x] Добавить unit, API, worker, contract и static PHP tests.
- [x] Добавить encrypted conversation read-model, item-scoped UI session,
  вкладку CRM с ответами/заметками/исходящими файлами и capability
  `command-files-v1`; подключить 90-дневную очистку к штатному worker.
- [x] Добавить dry-run ensure для `CRM_DYNAMIC_1134_DETAIL_TAB` с обязательным
  readback после bind; не выполнять bind при локальной разработке.
- [x] Выполнять `placement.get/bind` только через OAuth application context:
  webhook используется лишь для fail-closed проверки домена портала, а временный
  access token передаётся процессу через
  `SITE_SERVICE_REQUESTS_UI_BITRIX_APP_ACCESS_TOKEN`. Токен запрещено сохранять
  в `.env`, передавать аргументом CLI или выводить в лог; после запуска переменную
  окружения нужно удалить.
- [x] Разделить показ вкладки и UI-записи флагами `UI_ENABLED` и
  `UI_REPLIES_ENABLED`: первый допускает read-only историю, второй отдельно
  разрешает ответы, retry и внутренние заметки. Общий outbound не использовать
  как единственный UI write-gate.
- [x] Сделать вкладку единственным штатным CRM-каналом: добавить строгий
  `UI_WRITE_ALLOWED_USER_IDS`, capability `canAttachFiles`, скрыть legacy-поля
  ответа/истории из общей формы и отключить legacy `SEND` отдельным break-glass
  флагом по умолчанию.
- [ ] После отдельного разрешения применить migration, собрать UI, привязать
  вкладку, выполнить адресный snapshot backfill и controlled smoke; затем
  включить UI/attachments только после успешного readback.
- [x] Провести входящий controlled live smoke на тикетах 743–745: создать и
  перечитать карточки 373–375, назначить временно Тимуру при закрытой смене,
  доставить и проверить SHA-256 двух файлов тикета 744.
- [ ] После отдельных разрешений завершить оставшуюся часть controlled live smoke:
  штатный TimeMan/reconcile в рабочую смену, outbound/ack и close gate.
- [x] Добавить универсальные source identity, email endpoint, повторный CRM-gate,
  email worker, activity binding/readback и отдельный feature flag.
- [x] Добавить поля email-активности в ensure и регрессионные тесты, не включая
  их в общий обязательный mapping при выключенном email-intake.
- [ ] Применить email-source migration и поля 1134, зафиксировать baselines
  `shop`/`info`, выполнить два controlled email smoke и только затем включить
  email-intake.

# Review Notes / Risks

- Сайт и Bitrix24 Box — разные контуры; IDs пользователей сайта нельзя путать с
  ID сотрудников Box.
- Общая site-учётная запись `MASTER MOBILE` является администратором и не подходит
  для автоматических ответов. До outbound pilot нужен отдельный пользователь
  support-team с минимальными правами.
- REST-запись в multiple file field 1134 подтверждена на карточке 374: два файла
  перечитаны из CRM и совпали с исходными SHA-256. Production pilot выявил, что
  Disk object ID нельзя использовать как CRM file ID; карточка исправлена
  адресно, штатный backend-фикс подготовлен локально и требует отдельного deploy.
- Поле сделки `UF_CRM_1772784329053` наблюдается заполненным числом у последних
  интернет-заказов, но его смысл должен быть подтверждён контрольным заказом.
- Названия/enum IDs полей и стадий не хардкодятся: mapping пересобирается и
  проверяется перед rollout. Ensure dry-run/apply отклоняет malformed или
  неоднозначные `crm.type`, fields, enum, stages и form, проверяет обе формы
  pagination `next` и не выполняет writes после неизвестного readback.
- Timeline/уведомления не являются source of truth доставки ответа; требуется MID
  модуля support и последующий readback.
- Production change/readback 2026-08-25: единственный site Agent
  `mm_site_service_ticket_agent();` (ID `1327455`) переведён в `ACTIVE=N`;
  `emit=N`, `outbound=N`, а option служебного support user остаётся настроенной.
  ОТМЕНЕНО (2026-08-30): ограничение «до отдельного разрешения Agent остаётся
  выключенным» снято утверждённым production-пилотом.
- Production change/readback 2026-08-30: Agent `1327455` активен, site
  `emit=Y`/`outbound=Y`, backend outbound включён. Ответ из CRM-карточки `391`
  применён к тикету `760` как site-message `1782` ровно один раз, обратное событие
  обработано. После фильтрации `IS_LOG` authoritative history содержит клиентское
  сообщение `1780` и ответ поддержки `1782`; backend-watermark и карточка
  пересинхронизированы без системной строки `1781`.

# Tests

## Автоматические

- unit: event/command keys, HMAC, nonce replay, phone/e-mail normalization,
  exact order match, assignment, round-robin, SLA pause/resume, escalation;
- API: auth failures, idempotent event, payload conflict, file hash/size,
  command lease/ack, health redaction;
- worker: `crm.item.add` timeout/readback, update retry, duplicate event,
  ambiguous contact/order, TimeMan outage, close gate;
- contract: Python и PHP fixtures подписывают один и тот же canonical payload;
- PHP static: `php -l`, idempotent DDL dry-run, event extraction fixture,
  command duplicate fixture;
- regression: `tests/test_site_defect_workflow.py` и
  `tests/test_site_defect_archive.py`.

## Controlled live smoke

Первым реальным пилотом №3223 являются существующие тикеты сайта 743, 744 и 745.
Для них заполняются проверенные телефон, заказ и тип обращения, создаются карточки
процесса 1134. Распределение выполняется по штатному TimeMan/round-robin в рабочую
смену, Тимур Тибилов контролирует пилот. ОТМЕНЕНО (2026-08-30): общий intake и
outbound остаются выключенными до отдельного разрешения. После успешного входящего
пилота и отдельного утверждения site Agent, intake и outbound включены; для прямой
работы сотрудников создана отдельная support-группа с минимальными правами.

Только для пилотных тикетов 743–745: если при первичной синхронизации TimeMan не
видит открытой смены, временным ответственным карточки становится Тимур Тибилов
(`131016`), а backend-состояние остаётся `waiting`. При появлении открытой смены
штатный assignment reconcile передаёт карточку по обычному round-robin.

Ответственный за controlled live smoke — Тимур Тибилов (Bitrix user ID `131016`).

Входящая часть пилота 2026-08-25 завершена: события `743:1741`, `744:1742`,
`744:1744`, `745:1746` имеют статус `processed`; cases 3–5 связаны с карточками
373–375, назначены Тимуру и сохраняют backend-состояние `waiting`. Файлы
`2814203` и `2814204` тикета 744 загружены в служебную Disk-папку и в CRM-поле
карточки 374, после скачивания их SHA-256 совпали с исходными; spool очищен после
durable readback. Synthetic event/card не изменены. Постоянные backend writes,
outbound, site Agent и site emit/outbound остались выключены.

Только после отдельного разрешения:

1. создать тестовый тикет известного CRM-клиента и контрольного заказа;
2. проверить outbox, event, case, карточку 1134, историю и файл;
3. проверить три варианта TimeMan: Анна, Ариф, оба;
4. отправить тестовый текст из карточки и перечитать личный кабинет;
5. повторить command/ack и подтвердить отсутствие дубля;
6. попытаться закрыть карточку без ответа и проверить возврат стадии;
7. подтвердить, что category 56 и старые тикеты не изменились.

# Rollout

1. Зафиксировать backup изменяемых site-файлов и DDL/status до изменений.
2. Применить Alembic-миграцию `pricing-service`, API и worker с writes disabled.
3. Выполнить ensure dry-run против процесса 1134 и сохранить mapping/readback.
4. После отдельного разрешения добавить поля 1134 и проверить форму.
5. Развернуть site bridge с emit disabled и выполнить PHP/static/DDL dry-run.
6. Создать отдельного site support service user с минимальными правами.
7. Настроить secrets и IDs только в локальных env/Bitrix Option.
8. Включить ingest, затем один synthetic event без Bitrix write.
9. Включить Bitrix write только для одного тестового тикета.
10. Включить outbound reply только после успешного входящего smoke.
11. Включить новые тикеты сайта для пилота онлайн-отдела.
12. Первые 3 рабочих дня ежедневно проверять pending lag, ошибки, SLA и дубли.

Production status на 2026-09-01:

- active immutable release `unified-procurement-orders-20260901`, source commit
  `8b3cac24fd260f65db0f4e1c96cd8a5bf5812008`; manifest подтверждает обязательную
  базу №3223 `8e298a60f97063d42428c17d21ab93f800307494`;
- форма category 55 процесса 1134 применена с обязательным readback:
  `uxMismatches=[]`, управляемые legacy/technical fields в форме отсутствуют,
  неизвестные пользовательские разделы сохранены;
- `UI_REPLIES_ENABLED=true` для write-allowlist
  `115204,132252,12587,131016,130904`; список точно совпадает с
  текущим view-allowlist, но не расширяется автоматически;
  `LEGACY_FIELD_REPLIES_ENABLED=false`, исходящие вложения и backfill выключены;
- controlled smoke через тикет `760` создал ровно одну command `4`: site ACK
  подтвердил message `1788` с первой попытки, повтор того же `clientRequestId`
  вернул `duplicate=true`, обратное site-event обработано без ошибки;
- ОТМЕНЕНО (2026-09-01): не расширять allowlist до реального ответа
  клиента из личного кабинета; по решению пользователя право ответа
  открыто всем пяти уже допущенным к просмотру сотрудникам;
- worker-health исправен: нет pending events/commands, worker failure/stale и
  consecutive failures. Общий health остаётся `degraded` из-за двух старых email
  dead-letter от 2026-08-26 и одного существующего outbound `ticket_not_found`;
  эти ошибки не относятся к расширению UI-allowlist и требуют отдельного
  безопасного разбора без автоматической перепривязки писем или повторной отправки.

Rollback:

- сначала выключить site emit и outbound reply feature flags;
- остановить worker штатным способом;
- не удалять уже созданные карточки, ответы или файлы;
- восстановить site include/index из backup только если bridge влияет на страницу;
- таблицы и поля не удалять в аварийном rollback, чтобы сохранить audit/dedupe;
- личный кабинет продолжает работать через штатный модуль `support` независимо от
  состояния интеграции.

# Открытые дефекты 12.09.2026

Диагностика обращения «закрытые тикеты возвращаются в очередь», боевой контур,
read-only проверка БД `pricing` и REST того же webhook.

## D1. Проверка почтовой цепочки была невыполнима (исправлено)

Bitrix24 Box не отдаёт `THREAD_ID` через REST: поля нет в `crm.activity.get` и
`crm.activity.fields`, а `crm.activity.list` игнорирует и `select[]=THREAD_ID`, и
`filter[THREAD_ID]` (проверено: `filter[ID]=72985` с заведомо ложным
`filter[THREAD_ID]` вернул запись). Прежний `_activity_thread_id` подставлял вместо
отсутствующего поля ID самой активности, поэтому ответ поддержки сверялся с
идентификатором нового письма и всегда падал в `email_activity_thread_mismatch`.
Проходили только первые письма цепочки, где диспетчер тоже присылает activity id.
Следствие: `first_response_at` у `bitrix_mail` не выставлялся никогда — на
12.09.2026 он пуст у всех 5 почтовых case, и close gate возвращал карточку из
закрытой стадии бесконечно.

## D2. `_thread_has_service_binding` давал ложное подтверждение (исправлено)

Функция строилась на том же игнорируемом `filter[THREAD_ID]`, поэтому перебирала
все email-активности портала и подтверждала любую карточку, к которой привязано
хоть одно письмо. Удалена: проверяется только прямая привязка активности.

## D3. Close gate копил незакрываемые карточки (исправлено)

Пути «закрыть без ответа» не было вовсе. На 12.09.2026 залипло 25 карточек: 22
`site_ticket` (№744-774) и 3 `bitrix_mail` (items 381, 407, 416); сообщения
поддержки нет ни по одной. Простановка `first_response_at` задним числом
отклонена: она исказила бы SLA и скрыла клиентов без ответа. Добавлено
административное закрытие с обязательной причиной.

## D4. `bitrix_unavailable` скрывает настоящую причину (открыт)

Любой `RuntimeError` записывается как `bitrix_unavailable`. Наблюдаемый случай:
событие 102 `site-support:786:1872` висит в `retry` с 12 попытками, при этом
карточка 419 для тикета №786 создана корректно и с непустым
`ufCrm36Idempotencykey`. Нужен прогон `tasks.site_service_requests_worker` в
dry-run с трассировкой.

# Changelog
- 2026-09-16 — во вкладке «Переписка с клиентом» появился готовый ответ «Где
  заказ» со статусом доставки, треком и ссылкой отслеживания из сделки. При
  нескольких отправлениях подстановка блокируется предупреждением.
- 2026-09-16 — добавлен отлов дублей: второе обращение того же клиента по тому же
  заказу внутри окна закрывается с причиной «дубль», обе карточки получают ссылки
  друг на друга. По умолчанию выключено. Окно 5 минут проверено на всех трёх
  живых парах: 0:10 и 2:34 попадают, 9:20 остаётся открытой.
- 2026-09-16 — приветствие без продолжения перестало засчитываться как ответ
  клиенту: срок первого ответа не закрывается, ожидание клиента не снимается и
  карточка не уходит в «Ожидаем клиента». Подтверждение доставки ответа при этом
  не изменилось. Порог проверен разбором фактической переписки
  (`scripts/audit_site_service_greeting_replies.py`): из 51 исходящего ответа под
  фильтр попали ровно три — «саламчик», «Здравствуйте!» и «test».
- 2026-09-16 — добавлено автозакрытие обращений, где после нашего ответа клиент
  молчит заданное число рабочих дней (по умолчанию выключено). Производственный
  календарь вынесен в общий `app/services/work_calendar.py` и дополнен 2027 годом;
  расчёт срока реакции закупок переведён на него без изменения поведения. Для
  карточек, застрявших в «В работе» с уже отправленным ответом, добавлен разбор
  `scripts/backfill_site_service_stage_after_reply.py` с отдельным списком на
  ручную проверку.
- 2026-09-15 — стадии перестали зависеть от ручного переключения: подтверждённый
  ответ уводит карточку в «Ожидаем клиента», а сообщение клиента в закрытую карточку
  возвращает её в работу с пометкой в ленте и сбросом причины закрытия без ответа.
- 2026-09-15 — добавлено ожидание ответа после первого: поле «Клиент ждёт ответа с»,
  уведомление ответственному при новом сообщении клиента, автоматический возврат
  карточки из «Ожидаем клиента» в работу и эскалация руководителю по тому же сроку,
  что и для первого ответа. Раскладка карточки в ensure приведена к фактической:
  три раздела без технических полей и без полей движения посылки.
- 2026-09-15 — номер заказа из обращения нормализуется перед поиском сделки:
  сайт присылает «№ 246936» и «№ 240188 от 18.08.2026, 09:20», сделка хранит голый
  номер, поэтому заказ не находился, дубли контактов нечем было развести и карточка
  оставалась в `client_match_required` (21 карточка из 54). Добавлен
  `normalize_order_number` (префикс №/N/#, хвост «от <дата>, <время>»; буквенные
  номера вроде `РБГУ0066575` не меняются) и скрипт
  `scripts/resync_site_service_request_links.py` для пересборки связей у уже
  застрявших карточек: 10 из 17 разрешаются, остальным номер заказа не приходил.
- 2026-09-14 — возврат по обращению закреплён за реестром клиентских возвратов:
  добавлена вкладка карточки «Возврат товара» (`GET`/`POST .../items/{id}/returns`),
  регистрация из карточки привязывает возврат к обращению, тикету сайта и заказу,
  повтор трека не создаёт вторую запись. Ensure-скрипт placement обобщён на две
  вкладки одного приложения. Поля возврата в карточке признаны вторым источником
  правды и выводятся из формы.
- 2026-09-14 — закрыта гонка close gate с отправкой ответа: `first_response_at`
  появляется только после события `message.created` с сайта (задержка 1-2 минуты),
  поэтому закрытие сразу после «Отправить клиенту» откатывалось зря (тикеты №746,
  №767, №769, №771 за 14.09). Гейт теперь учитывает команду ответа (`applied` —
  закрытие принимается, `pending`/`leased` в пределах аренды — решение
  откладывается) и объясняет каждый откат комментарием в карточке.
- 2026-09-12 — исправлены D1-D3 по обращению «закрытые тикеты возвращаются в
  очередь»: `_activity_thread_id` больше не подставляет ID активности вместо
  отсутствующего `THREAD_ID`, REST-сверка цепочки стала мягкой, перебор цепочки
  удалён как дающий ложное подтверждение, добавлено административное закрытие с
  обязательной причиной `close_without_response_reason` и полями case
  `closed_without_response_at`/`close_without_response_reason` (миграция
  `c9e1a3b5d7f2`). D4 остаётся открытым.

- 2026-09-01 — после успешного controlled smoke по решению пользователя
  UI write-allowlist расширен до всего текущего view-allowlist:
  `115204,132252,12587,131016,130904`; автоматический допуск новых
  пользователей запрещён, legacy-ответы, вложения и backfill остались
  выключены. Live readback подтвердил `canReply=true` и `canAttachFiles=false`
  для всех пяти пользователей, `canReply=false` и mutation `403` вне списка;
  очереди пусты, worker исправен, клиентские сообщения не отправлялись.
- 2026-09-01 — по явному разрешению выполнен production-пилот единой вкладки:
  release `8e298a6`, форма 1134 и readback применены, UI-запись включена только
  для `115204,131016`, legacy/attachments выключены; тикет `760` получил один
  ответ command `4` / message `1788`, idempotent retry не создал дубль, обратное
  событие обработано. Расширение allowlist отложено до реального ответа клиента.
- 2026-09-01 — production readback выявил ограничение Bitrix Box: для уже
  существующего dynamic user field портал немедленно возвращает `showInList=Y`
  после успешного update с `N` или `false`. Ensure сохраняет fail-closed
  `showFilter=N`, `editInList=N` и удаление legacy-полей из формы, а неизменяемый
  `showInList` сообщает как advisory; новые поля по-прежнему создаются скрытыми.
- 2026-08-31 — утверждён единый UX переписки: вкладка процесса 1134 становится
  единственным штатным CRM-каналом, старые поля ответа и полной истории скрываются
  и не обрабатывают `SEND` без отдельного break-glass флага; UI-записи защищены
  fail-closed allowlist с пилотными ID `115204` и `131016`, а вложения остаются
  отдельной выключенной capability.
- 2026-08-31 — разрешён UI-only rollout вкладки процесса 1134: настроить
  production env, выполнить bind и readback, включить историю только для чтения;
  UI-ответы, внутренние заметки, вложения, backfill и новые отправки клиентам
  остаются выключены отдельным `UI_REPLIES_ENABLED=false`.
- 2026-08-30 — production backend переключён на merge commit `cc02ca8`, Alembic
  применён до `e4a6c8d0f2b4`, release-smoke и GitHub CI прошли; UI/attachments
  flags, placement bind и snapshot backfill не включались. Read-only rollout
  выявил, что Bitrix запрещает `placement.*` для webhook с
  `WRONG_AUTH_TYPE`; ensure переведён на краткоживущий OAuth application context
  без хранения или логирования access token.
- 2026-08-30 — утверждена встроенная вкладка «Переписка с клиентом» для процесса
  1134: ответ без перехода на сайт, имя сотрудника внутри CRM, единый бренд для
  клиента, исходящие вложения и внутренние заметки; encrypted read-model хранится
  90 дней после подтверждённого закрытия, старые CRM-поля остаются fallback.
- 2026-08-30 — утверждён и выполнен production-пилот двустороннего обмена:
  активированы site Agent/intake/outbound и backend outbound, ответ CRM -> тикет
  `760` применён без дубля, обратное событие обработано; после `IS_LOG`-фикса
  карточка `391` пересинхронизирована по сообщениям `1780` и `1782`.
- 2026-08-30 — ОТМЕНЕНО 2026-08-31: правило о двух равноправных штатных каналах
  заменено единой вкладкой CRM, чтобы исключить неоднозначную отправку через
  старые поля. Прямой исходный тикет сохранён только как ручной fallback;
  системные строки `b_ticket_message.IS_LOG` исключены из бизнес-истории.
- 2026-08-29 — ежедневная сводка ограничена письмами без ответа и просроченными
  обращениями сайта; добавлены простые формулировки и прямые ссылки для ответа.
- 2026-08-28 — утверждён ежедневный отчёт по всем остающимся без ответа письмам
  и обращениям сайта; отчёт по интернет-заказам остаётся отключён.
- 2026-08-27 — для просроченных писем и обращений сайта в напоминания добавлены
  прямые ссылки на карточку CRM и исходное обращение.
- 2026-08-27 — напоминания о просроченном ответе на обращения и письма переведены
  на простой русский язык без технических сокращений; в сообщении явно указано,
  что нужно проверить и ответить клиенту.
- 2026-08-26 — assignment reconcile переведён на semantic diff, чтобы исключить
  неизменившиеся минутные записи и снизить нагрузку на Bitrix.
- 2026-08-26 — для преобразуемых Bitrix-вложений выбран persisted baseline/delta
  guard; cleanup карточки 379 выполняется отдельно после проверенного deploy.
- 2026-08-26 — стабилизирован worker №3223: после lock принят сдвиг 20 секунд,
  HTML 403 получает retries 2/5/10, JSON access error остаётся fail-closed;
  добавлены durable heartbeat со stale-порогом 180 секунд, health-alerts worker/file
  и commit-before-attach guard с лимитом 50 CRM-файлов.
- 2026-08-25 — принят следующий этап почтового диспетчера: сервисные
  email-обращения `shop` и `info` подключаются к процессу 1134 с точным CRM-gate,
  отдельным feature flag и ответом в исходной email-цепочке.
- 2026-08-25 — read-only аудит карточек 374–375 подтвердил дубли CRM-контактов,
  которые однозначно разрешаются заказами 241094/241573. Зафиксирован безопасный
  fallback: единственная пара `контакт + точный заказ` разрешает дубль, несколько
  пар остаются fail-closed. Пользовательские подписи сделаны действие-ориентированными,
  телефон/e-mail и следующее действие — многострочными; production не изменён.
- 2026-08-25 — завершена входящая часть реального пилота 743–745: четыре события
  обработаны, карточки 373–375 назначены Тимуру в состоянии `waiting`, два файла
  карточки 374 подтверждены обратным скачиванием и SHA-256; synthetic и все
  постоянные write/outbound/Agent-флаги не изменены. Live smoke выявил разделение
  Disk object ID и CRM file ID; карточка исправлена адресно, штатный backend-фикс
  подготовлен локально и ожидает отдельного deploy.
- 2026-08-25 — для пилотных тикетов 743–745 утверждён временный ответственный
  Тимур Тибилов при отсутствии открытой смены; последующая передача выполняется
  штатным TimeMan/round-robin, outbound остаётся выключенным.
- 2026-08-25 — тикеты 743–745 выбраны первым реальным пилотом №3223 для обработки
  сотрудниками без включения общего intake и автоматических ответов клиентам.
- 2026-08-25 — production readback подтвердил, что признак закрытия тикета хранится
  в `b_ticket.DATE_CLOSE`, а не в отсутствующем `CLOSED`; site bridge из commit
  `5d8e54e` развёрнут по отдельному разрешению с SHA-256
  `9a64fb0934bf2a30ab69eb8567f1ffbe97e7d546fdedbcbcab43b4043336ca11` и PHP
  lint/readback. Backup старой версии сохранён с суффиксом
  `.bak.20260825T014625+0300.5d8e54e`; Agent `1327455`, `emit` и `outbound`
  остались выключены. Controlled live smoke требует отдельного разрешения.
- 2026-08-25 — ответственным за controlled live smoke и за задачу Bitrix24
  №3223 назначен Тимур Тибилов (Bitrix user ID `131016`).
- 2026-08-25 — по отдельному production-разрешению единственный site Agent
  `mm_site_service_ticket_agent();` (ID `1327455`) выключен с обязательным
  readback; site `emit/outbound` остались `N`. Backend commit `3e997f3` развёрнут
  штатным `/usr/local/sbin/mm-pricing-service-release`: active source и Alembic
  head подтверждены, systemd и release-smoke healthy. Backend-флаги сохранены как
  `ingest=true`, `Bitrix writes=false`, `outbound=false`; synthetic event остался
  в `retry` с `attempts=2`, а synthetic contact `70319` и item `372` остались
  единственными. Worker, site emit и outbound не запускались.
- 2026-08-24 — production UX apply процесса 1134 завершён с обязательным
  readback: процесс и рабочая воронка переименованы, 58/58 типов и CRM-настроек
  полей проверены, добавлено employee-поле согласующего, технические поля скрыты,
  enum ID `402–407` сохранены; форма и стадии архивной воронки 56 не изменились.
  Финальная локальная регрессия: `2976 passed`, `13 skipped`; Ruff, Black,
  OpenAPI check и docs validation прошли.
- 2026-08-24 — production readback подтвердил, что Bitrix Box сохраняет русскую
  `editFormLabel`, но возвращает `null` для `listColumnLabel`/`listFilterLabel`
  всех полей процесса 1134; ensure больше не блокирует форму на этом системном
  ограничении и сохраняет расхождения как advisory.
- 2026-08-24 — принято решение до пилота с сотрудниками завершить UX-аудит процесса
  1134: переименовать его в «Сервисные обращения сайта», рабочую воронку 55 — в
  «Рабочие обращения сайта», проверить типы и enum-справочники, оставить на форме
  только понятные русские пользовательские названия и скрыть технические поля.
- 2026-08-24 — contact readback приведён к фактическому контракту
  `crm.contact.get`: поле `ACTIVE` у CRM-контакта не требуется, а защита от
  ошибочной связи остаётся fail-closed по строгим `ID/id` и `COMPANY_ID/companyId`.

- 2026-08-24 — production env preflight подтвердил boolean `ACTIVE=true` в
  `user.get`; pilot-user validation теперь строго принимает только boolean или
  строки `Y/N` и по-прежнему отклоняет conflicting aliases. Пример списка первой
  линии исправлен на JSON-массив, совместимый с Pydantic dotenv-source.
- 2026-08-24 — production read-only preflight подтвердил, что `crm.type.get`
  принимает внутренний `id`, а не `entityTypeId`. Ensure discovery переведён на
  form-encoded `crm.type.list` с `filter[entityTypeId]`, строгой проверкой ровно
  одной пары `id/entityTypeId`, обеих форм pagination `next`, циклов и лимита
  100 страниц. Поскольку `userfieldconfig.list` на Box не возвращает enum,
  enumeration fields дополнительно перечитываются через `userfieldconfig.get` с
  проверкой identity; malformed readback завершается fail-closed до любых writes.
- 2026-08-24 — для production-candidate добавлена no-op Alembic merge migration
  `8c0d2e4f6a57`, объединяющая активную production-голову `2d6f8a0c4b13` с
  проверенной цепочкой №3223 `7b9d1f3a5c46`; schema fork при rollout исключён.
- 2026-08-24 — ambiguous-commit и post-upload spool cleanup переведены с path-only
  readback на блокировки владельца `case -> file` по durable IDs, поэтому
  незакоммиченный конкурентный PUT не теряет payload. Site command marker теперь
  проверяется и записывается под MySQL advisory lock с повторной проверкой lease,
  а персональные escalation/needs-attention уведомления получают стабильный Bitrix
  `TAG`, исключающий второй экземпляр после неоднозначного timeout. Регрессионные
  тесты добавлены; адресный suite №3223 и полный локальный `pytest` пройдены
  (`2804 passed`, `11 skipped`; PHP runtime-тесты пропущены из-за отсутствия
  локального PHP runtime).
- 2026-08-24 — command lease теперь сверяет расшифрованный payload с
  `reply_sha256`; quarantine сохраняет `max(outbound checkpoint)`, но всегда
  деградирует health для последней повреждённой command. CRM deal/contact/item/
  Disk и file-field ID aliases, а также aliases ID/активности/имени пилотных
  пользователей проверяются строго и fail-closed. ACK не откатывает более новый
  outbound checkpoint; stale success также не стирает его error. Spool-path
  сохраняется до успешного durable cleanup, а cleanup повторно проверяет
  владение path под row lock и не удаляет конкурентно повторно загруженный
  payload. Ensure
  строго отклоняет conflicting field IDs, stage semantics и malformed/
  conflicting `xmlId/XML_ID` даже у переименованного поля до writes.
  Регрессионные тесты добавлены, но пока не запускались по решению пользователя.
- 2026-08-24 — outbound reply ограничен 200 000 символами: oversized `SEND`
  сохраняет action и получает durable `reply_text_too_long`, а старые oversized
  command-записи карантинируются до построения response model. HTTP `413` удалён
  из terminal site-outbox ответов и остаётся retryable. Enum aliases ensure и
  assignment checkpoints дополнительно сделаны fail-closed/монотонными;
  регрессионные тесты добавлены, но пока не запускались по решению пользователя.
- 2026-08-23 — command lease/quarantine приведены к единому порядку row locks
  `case -> command`; notification result и company ID строго валидируются,
  а ensure требует точный строковый XML ID целевого поля и stage текущего
  entity/category scope. Регрессионные тесты добавлены без запуска до общего
  финального прогона.
- 2026-08-23 — повторный аудит сделал TimeMan/contact/stage/timeline/form readback
  строго типизированным и сохранил durable `reply_text_empty`, если сотрудник снимает
  пустой `SEND` конкурентно с записью ошибки. Добавлены регрессионные тесты без
  запуска до общего финального прогона.
- 2026-08-23 — исправлен синтаксис request-type mapping в ensure; terminal
  reconcile больше не очищает конкурентный `SEND`, reply action/deal title/timeline
  comment/ensure field scalars валидируются fail-closed, process IDs не усекают
  дробные значения, а `reply_text_empty` сохраняется до валидного повторного SEND.
  Регрессионные тесты добавлены без запуска до общего финального прогона.
- 2026-08-23 — outbound reply readback ограничен строковым типом, пустой `SEND`
  получает durable `reply_text_empty`, success-checkpoint стал монотонным, а
  worker/ensure используют строгий parser pagination offsets. Пустой document root
  в PHP bridge теперь остаётся retryable `file_api_unavailable`; регрессионные
  тесты добавлены, но их запуск отложен до единого финального прогона.
- 2026-08-23 — дополнительный статический hardening сделал ensure/worker/PHP
  bridge fail-closed на malformed type/stage/enum/form/contact/deal/item/timeline/
  Disk readback; восстановление outbound теперь подтверждает SHA-256 текста и
  sync-error; same-tick selection failure больше не скрывается успехом предыдущей
  карточки, повреждённая последняя command сразу деградирует health, а durable
  `needs_attention` не откатывается при сбое внутреннего уведомления. Site outbox
  строго проверяет типы event/command/ACK JSON, не маскирует SQL failure как пустой
  readback, terminal-классификацию ограничивает `400/409/413/415/422` и неизвестные
  локальные ошибки не ACK-ает; root Disk folder обязателен и положителен. Тесты
  намеренно отложены до единого финального прогона.
- 2026-08-23 — form ensure теперь отклоняет malformed readback до слияния/записи;
  временные file read/hash failures остаются в retry, UTF-8 filename сохраняется
  между event/upload, старый file placeholder не потребляется новым snapshot, а
  файловый spool не выполняет SQL после атомарной записи; поздняя file error
  получила отдельный Bitrix delivery-checkpoint/readback.
- 2026-08-23 — placeholder удалённой записи `b_file` получил безопасное
  восстановление всей файловой меты; первичный outbound action и очистка assignee
  теперь требуют присутствия полей в readback и fail-closed при их отсутствии.
- 2026-08-23 — дополнительный hardening-аудит закрыл ложный event hash-conflict
  при изменении текущих полей тикета, сделал `file_unavailable` достижимым,
  добавил ротацию lease-token/stale-ACK guard, quarantine повреждённой command,
  timeout-readback контакта и fail-closed pagination ensure/deal/timeline.
- 2026-08-23 — финальная миграция исправляет ошибочный marker-backfill для всех
  commands старого worker. ОТМЕНЕНО 2026-08-23 более поздним решением о hash-guard:
  terminal-команда с незавершённой в БД очисткой больше не считает пустой action
  достаточным доказательством владения изменённым текстом и восстанавливает `SEND`.
  Health последней failed delivery сохраняется, даже если новая command ожидает
  отправки.
- 2026-08-23 — закрыты две межworker-гонки: устаревшая outbound-попытка не очищает
  новый повторный `SEND`, а старый failure-checkpoint не затирает более новый success.
- 2026-08-23 — ранние assignment/outbound failures включены в durable checkpoint;
  stale assignment failure очищается после первого ответа, а повторный `SEND` того
  же текста создаёт новую command только после терминальной предыдущей команды.
- 2026-08-23 — ACK и outbound унифицированы по порядку row locks `case -> command`;
  ошибки БД worker теперь оставляют durable assignment/outbound health-checkpoint.
- 2026-08-23 — для outbound выбран MVP hash-guard до/после очистки; новый Bitrix
  UI/action ID не добавляется.
- 2026-08-23 — локально устранены concurrency/failure findings: assignment и
  outbound обрабатываются по одной карточке под транзакционными блокировками,
  эскалация получила durable delivery-state, body-limit проверяется до FastAPI,
  а readback очистки стал fail-closed; добавлена отдельная backfill-миграция.
- 2026-08-23 — исправлен контракт timeline для smart-process, outbound failure
  checkpoint и retry незавершённой эскалации после первого ответа.
- 2026-08-22 — решено закрыть все findings повторного code review; признаком
  принятия обращения считать уход карточки из `NEW`.
- 2026-08-22 — выполнен повторный локальный hardening-review: усилены ограничения
  входящих body, файловые ошибки и readback, последовательность событий одного
  тикета, worker/round-robin/SLA, outbound ack/status, user preflight, безопасное
  слияние формы и PHP-контракт `CTicket::Set`; live smoke и внешние изменения не
  выполнялись.
- 2026-08-22 — локально реализованы intake API, durable-модели, worker, ensure-скрипт,
  cron-template, site PHP bridge, SUPPORT fields, OpenAPI и автоматические проверки;
  production, Bitrix24 и `master-mobile.ru` не изменялись, controlled live smoke
  остаётся отдельным rollout-гейтом.
- 2026-08-22 — создана Implementation Spec для задачи №3223 на основании OnePage,
  живого read-only обследования модуля support, процесса 1134 и TimeMan; зафиксирован
  асинхронный двусторонний контур с durable dedupe, SLA и безопасным rollout.
