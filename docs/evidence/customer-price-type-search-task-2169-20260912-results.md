# Локальная проверка поиска типов цен по задаче №2169

Дата проверки: 2026-09-12.

- UI: 5 адресных тестов прошли; TypeScript/Vite production build завершился с кодом 0.
- Backend: адресные тесты API и авторизации прошли с кодом 0.
- Ruff: выбранные backend-файлы прошли без замечаний.
- Документация: `validate_project_docs.py pricing-service` проверил 82 документа и 31 spec.
- `git diff --check`: ошибок пробелов в файлах этапа не обнаружено.

## Production-деплой

Дата проверки: 2026-09-12, 22:57 MSK.

- Канонический CI `34714308766` на merge-коммите
  `909c50932ae989d82db6404654b706a7c5a0847d` прошёл полностью.
- Guarded deploy завершился с кодом 0; активен immutable release
  `customer-price-search-2169-20260912` на том же commit, freeze отсутствует.
- `pricing-service.service` имеет состояние `active`, `ExecMainStatus=0`.
- В активном frontend-bundle подтверждены строки «Поиск по всему портфелю» и
  «Клиент найден в портфеле».
- Read-only запрос production-БД нашёл `РБ002793`: «Карпов Анатолий Республика
  Беларусь», текущий тип `4.Золотой`, `source_status=conflict`,
  `case_type=data_check`, причина `source_mismatch`.
- Первый запуск сборки был остановлен до переключения из-за локального
  игнорируемого symlink `.venv` в старом worktree. Production остался на
  прежнем релизе; повтор выполнен из нового чистого detached worktree.

Проверка реальной Bitrix-сессии Арсена не выполнялась: production asset,
backend/runtime и данные подтверждены, но пользовательское отображение под его
ролью остаётся проверить Арсену в интерфейсе. Публикация ответа в задачу не
выполнялась.
